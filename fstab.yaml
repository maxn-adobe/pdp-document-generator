# Registers this repo as an AEM Edge Delivery Services (EDS) site so Code Sync
# serves the built /dist tool from the code bus. The mountpoint target is not
# fetched (the tool is static) — it only needs to be present and valid.
mountpoints:
  /: https://content.da.live/maxn-adobe/pdp-document-generator/
